# 나율이의 하브루타 🚌📚

5세 나율이를 위한 하브루타 독서 대화 앱 (PWA). 엄마가 책을 읽어주고, AI 짝꿍이 음성으로 하브루타 5단계 대화를 진행합니다. GPTs 프롬프트 「나율 전용 하브루타 GPT v1.0」을 그대로 이식했습니다.

## 주요 기능

- 한글책 / 영어책 모드 (홈 화면 큰 버튼)
- 📖 책 검색: 제목 + 출판사·시리즈·저자로 검색 → 동일 제목 책 중에서 선택 (네이버 책 API / Google Books)
- 👂 같이 듣기: 엄마가 읽어주는 동안 음성을 실시간 전사 → AI가 실제 읽은 내용을 근거로 질문
- ⏭️ 읽기 생략: 검색으로 책 소개가 확인된 책은 읽지 않고 바로 하브루타 시작 가능
- 읽기 대기 화면: 책 읽는 동안 AI가 끼어들지 않음 → [다 읽었어!] 버튼으로 대화 시작
- 음성 대화: 마이크 버튼(Web Speech STT) + AI 답변 자동 읽어주기(TTS, 문장별 한/영 음성 자동 전환)
- 영어책 모드에서 아이가 한국어로 답할 수 있게 듣기 언어 토글 제공
- 하브루타 5단계 진행도(엄마용 작은 점 표시, 아이에겐 단계 언급 안 함)
- 오늘 짧게 / 보통 / 오늘 깊게 토글
- 엄마 설정: 사고 Lv / 한글 K레벨 / 영어 E레벨 개별 조정 (프롬프트에 동적 주입)
- 대화 종료 후 엄마 피드백 + 독서 기록 자동 저장 (Supabase, 미설정 시 기기 저장)
- PWA: 안드로이드/아이폰 홈 화면에 앱으로 설치 가능

## 배포 방법 (GitHub 웹 UI + Vercel)

1. GitHub에서 새 저장소 생성 (예: `nayul-havruta`)
2. 이 zip의 압축을 풀고 **폴더 안의 파일 전체**를 GitHub 웹 UI에 드래그 앤 드롭 → Commit
3. Vercel → Add New Project → 해당 저장소 Import
4. **Environment Variables** 추가:

| 이름 | 값 | 필수 |
|---|---|---|
| `OPENAI_API_KEY` | OpenAI API 키 | ✅ 필수 |
| `OPENAI_MODEL` | 기본 `gpt-4.1-mini` (생략 가능) | 선택 |
| `SUPABASE_URL` | Supabase 프로젝트 URL | 선택 (기록 서버 저장용) |
| `SUPABASE_SERVICE_ROLE_KEY` | Supabase service_role 키 | 선택 |
| `GOOGLE_TTS_API_KEY` | Google Cloud TTS API 키 | 선택 (한글책 목소리 품질 ↑) |
| `NAVER_CLIENT_ID` | 네이버 개발자센터 앱 Client ID | 선택 (책 검색 정확도 ↑) |
| `NAVER_CLIENT_SECRET` | 네이버 앱 Client Secret | 선택 |

**Google TTS 키 발급**: Google Cloud Console → 프로젝트 선택 → "Cloud Text-to-Speech API" 검색 후 **사용 설정** → API 및 서비스 → 사용자 인증 정보 → **API 키 만들기**. ⚠️ 프로젝트에 **결제 계정이 연결**되어 있어야 합니다 — 예전 403 오류가 바로 결제 미연결이 원인이에요. 무료 할당량(월 백만 자 수준)이 있어 하루 한 권 사용량이면 사실상 무료입니다.

책 검색은 네이버 키가 있으면 네이버 책 검색(한국 책 정확도 높음), 없으면 Google Books로 자동 폴백합니다. 네이버 키는 developers.naver.com → 애플리케이션 등록 → 검색 API 선택으로 무료 발급됩니다.

5. Deploy → 완료!

Supabase를 쓰려면 `supabase.sql`을 Supabase SQL Editor에서 실행해 테이블을 먼저 만들어 주세요. 환경변수를 넣지 않아도 앱은 동작하며, 기록은 기기(localStorage)에 저장됩니다.

## 앱으로 설치하기

- **안드로이드 (크롬)**: 사이트 접속 → 메뉴(⋮) → "홈 화면에 추가" 또는 "앱 설치"
- **아이폰 (사파리)**: 사이트 접속 → 공유 버튼 → "홈 화면에 추가"

## 사용법

1. 홈에서 책 제목(+출판사·시리즈) 입력 → [🔍 찾기] → 우리 책 선택 → 한글책 또는 English Book
   (검색 없이 제목만 쓰고 바로 시작해도 됩니다)
2. 읽기 화면에서 [👂 같이 듣기 시작]을 켜고 엄마가 책을 소리 내어 읽어줌 — AI가 내용을 실시간으로 들어요
   (이미 잘 아는 책이면 [⏭️ 읽기 없이 바로 이야기] 선택 가능)
3. [다 읽었어! 이제 얘기하자] → AI가 첫 질문을 음성으로
4. 나율이가 🎤 버튼 누르고 말하기 → 다 말하면 버튼 다시 누르기
5. 끝낼 땐 [🌙 오늘은 여기까지] → 부드러운 마무리 인사
6. [👩 엄마 피드백 + 기록 저장] → 강점/다음 목표/다음 질문 확인, 기록 자동 저장

엄마가 마이크로 "오늘은 선택형 질문 많이 해줘" 같은 지시를 말해도 즉시 반영됩니다.

## 알아두기

- 음성 인식은 안드로이드 크롬에서 가장 잘 동작합니다. 아이폰은 사파리로 열어 주세요. 미지원 브라우저에서는 키보드 입력 모드로 자동 안내됩니다.
- TTS 음성 품질은 기기 내장 음성을 사용합니다. 안드로이드는 설정 → 텍스트 읽어주기에서 Google TTS 한국어/영어 음성을 설치하면 훨씬 자연스러워요.
- AI가 모르는 책은 내용을 지어내지 않도록 프롬프트에 고정되어 있습니다. 잘 알려지지 않은 책은 대화 시작 후 엄마가 줄거리를 한두 문장 말해주면(마이크 또는 키보드) 그걸 기준으로 대화합니다.

## 파일 구조

```
app/
  page.js            홈 (모드 선택)
  session/page.js    읽기 대기 → 음성 대화 → 마무리
  parent/page.js     엄마 설정 (레벨 조정)
  records/page.js    독서 기록 목록
  api/chat/route.js    OpenAI 대화 API
  api/report/route.js  엄마 피드백 + 기록 생성 API
  api/records/route.js Supabase 기록 저장/조회 API
lib/prompt.js        하브루타 시스템 프롬프트 (GPTs v1.0 이식)
public/              PWA manifest, 아이콘, 서비스워커
supabase.sql         기록 테이블 생성 SQL
```

## 체인지로그

### v1.3.0 (2026-07-09)
- 🔊 하이브리드 TTS: 한글책과 영어책의 목소리 엔진을 각각 설정
  - 한글책: 기기 음성 / Google 한국어 음성(Chirp3-HD·Neural2·Wavenet 10종, 한국어 발음 최고) / OpenAI 음성
  - 영어책: 기기 음성 / OpenAI 음성 (원어민급)
  - Google TTS 실패 시 OpenAI → 기기 음성 순으로 자동 폴백
  - 엄마 설정에서 조합별 ▶ 들어보기 샘플 재생 (기기 음성 샘플 포함)
  - 새 환경변수: GOOGLE_TTS_API_KEY (선택)


### v1.2.0 (2026-07-09)
- 🔊 OpenAI TTS 통합 (gpt-4o-mini-tts): 기기 음성보다 훨씬 자연스러운 AI 목소리 지원
  - 엄마 설정 → 목소리 엔진(기기 음성 / OpenAI 음성) 전환, 13개 음성 선택 (marin·cedar 최고 품질 권장)
  - ▶ 한국어/영어 들어보기 버튼으로 앱 안에서 샘플 청취 후 선택
  - 유아 대상 다정한 말투 instructions 내장 (한글/영어 모드별)
  - OpenAI TTS 실패 시 기기 음성으로 자동 폴백
  - 비용: 약 $0.015/분 (하루 한 권 대화 기준 월 수백 원 수준), 별도 환경변수 불필요 (OPENAI_API_KEY 공용)


### v1.1.1 (2026-07-09)
- 🎤 아이 말하기 도중 끊김 수정: 아이가 말 중간에 뜸을 들이면(1~2초 침묵) 브라우저 음성 인식이 조기 종료되어 문장이 잘린 채 전송되던 문제 해결
  - continuous 모드 + 침묵으로 인식이 끊겨도 자동 재시작(전송하지 않음), 말한 내용은 계속 누적
  - 전송 시점: 마이크 버튼을 다시 눌렀을 때, 또는 말한 내용이 있는 상태로 7초간 완전 침묵 시 자동 전송
  - 누적 중인 말이 화면에 실시간 표시되어 다 담겼는지 확인 가능
- 대화 종료 시 마이크 자동 재시작이 남지 않도록 정리


### v1.1.0 (2026-07-09)
- 👂 같이 듣기: 읽기 화면에서 엄마의 낭독을 실시간 음성 전사(자동 재시작 포함, 최대 15,000자) → 대화 시작 시 AI에게 전달되어 실제 읽은 내용 기반 질문 생성
- 📖 책 검색: 홈에 출판사·시리즈·저자 입력칸 추가, 네이버 책 API(키 설정 시) / Google Books(폴백) 검색 → 동일 제목 도서 중 선택 UI
- ⏭️ 읽기 생략: 검색으로 책 소개가 확인되면 읽기를 건너뛰고 바로 하브루타 시작 가능
- 프롬프트: 검색 확인된 책 정보 주입(소개에 없는 내용 단정 금지), 전사 오인식 문맥 복원 규칙 추가


### v1.0.0 (2026-07-08)
- 최초 릴리스
- GPTs 「나율 전용 하브루타 GPT v1.0」 프롬프트 전체 이식 + 음성 앱 출력 규칙 추가
- 한글책/영어책 모드, 읽기 대기 화면, 음성 대화(STT/TTS), 5단계 진행 점 표시
- 사고/한글/영어 레벨 설정, 짧게/보통/깊게 토글
- 엄마 피드백 + 독서 기록 (Supabase 또는 localStorage)
- PWA 설치 지원 (manifest + 서비스워커 + 아이콘)
